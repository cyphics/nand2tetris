class Bank{
	static int nAccounts;
	static int bankCommission;

	field int id;
	field String owner;
	field int balance;

	method int commision(int x) {}
	method void transfer(int sum, BankAccount from, Date when){
		var int i, j;
		var Date due;
		let balance = (balance + sum) - commission(sum * 5);
		return;
	}
}
